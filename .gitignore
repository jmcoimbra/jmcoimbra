_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Git worktrees criadas pelo Claude Code. Sao checkouts descartaveis do proprio
# repo: aparecem como untracked no status e um "git add -A" distraido commitaria
# o diretorio inteiro dentro dele mesmo.
.claude/worktrees/
